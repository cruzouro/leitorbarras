function goBack() {
    window.location.href = '/'; // Redireciona para a página inicial
}

function openRetry() {
        window.location.href = '/retry'; // Redireciona para retry na mesma janela
}